# 重置次数展示验收

日期：2026-09-07。所有自动化使用固定预览、临时数据目录和假 Codex 子进程，不访问真实账号。

## 构建与回归

- Release 构建通过，保持 Swift 6 严格并发及 macOS 14 部署目标。
- `VEYRA_DERIVED_DATA_PATH=/tmp/veyra-reset-tests ./scripts/test.sh`：90 项 XCTest、8 项隔离应用诊断全部通过。
- 新增测试覆盖精确到期分组、排序、重复 ID、未知有效期、非法数值、非可用状态、明细与总数不一致、到期边界、缺少额度窗口、独立发布、网络失败和账号／配置失效。
- 首次构建受沙箱中的 Swift 宏插件限制；正常环境构建后，原有 `build/` 下的测试包加载失败。使用独立构建目录完成测试，没有修改工程配置绕过检查。

## 布局与真实菜单

27 个场景 × 浅色、深色、两种高对比度，共 108 张布局预览通过尺寸检查。重置卡片的多批次、已到期、未知有效期、零次、接口不可用和无额度窗口场景均有独立夹具。

真实 MenuBarExtra 在浅色、深色、两种高对比度及浅／深色减少透明度六种测试外观下，96 个到期批次的顶部、中部、底部、快速滚动和同一窗口收缩检查均通过。正文为 2702pt，窗口限制为 1836pt；切换到单任务后收缩至 533pt。辅助功能读取确认总次数和精确到秒的到期提示存在。

动态到期联调使用命令行参数临时指定假 Codex 与临时目录。打开菜单并点击校准后，总数从“2 次”在到期后自动变为“待校准”，已到期批次标注“已到期 · 待校准”，未来批次仍保留。全过程仅调用一次 `account/rateLimits/read`，未发生兑换或自动重新校准。

## 产物与限制

- 布局预览及尺寸：`build/reset-credits-previews/`。
- 原生菜单布局位图、辅助功能文本、滚动和动态到期结果：`build/reset-credits-native/`。
- 测试日志：`/tmp/veyra-reset-clean-tests.log`；构建日志：`/tmp/veyra-reset-release.log`。
- 当前屏幕录制权限为关闭；原生菜单布局位图不包含完整 WindowServer 合成，不能据此声明原生玻璃及系统级外观的视觉验收通过，仍需人工目视确认。测试未修改系统外观或权限，也未替换用户正在运行的 Veyra 进程。
